# Flutter
.dart_tool/
.packages
.pub/
build/
*.iml
*.log

# IDE
.idea/
.vscode/
*.swp
*.swo

# macOS
.DS_Store
macos/Pods/
macos/Flutter/ephemeral/

# Android
android/.gradle/
android/app/build/
android/build/
# Die drei Regeln oben sind auf das Wurzel-android/ verankert und greifen NICHT
# in packages/*/android/. Die Plugin-Pakete (icd_sms, icd_netinfo) legen dort
# beim Bauen ebenfalls .gradle/ und build/ an — ohne diese beiden Zeilen landen
# ein paar hundert KB Build-Müll im Repo.
.gradle/
packages/*/android/build/
*.apk
*.aab

# iOS
ios/Pods/
ios/Flutter/ephemeral/

# Windows
windows/flutter/ephemeral/

# Backup files
*.bak
*.bak.*
*.bak2
*.bak3
*.bak4
*.bak5
*.bak6
*.bak7
*.bak8
*.bak9

# SSH keys - NEVER commit
vps_icd360sev_icd360s.de
vps_icd360sev_icd360s.de.pub
# any file matching the production host name (current SSH key lives at <root>/icd360sev.icd360s.de)
*icd360sev.icd360s.de
*icd360sev.icd360s.de.pub
# new server SSH key (migration 2026-06-02)
icd360sev_new_*
# gateway SSH key (migration 2026-06-02)
icd360sev_gateway_*
# self-hosted runner VM SSH key (2026-06-18, 10.10.10.60)
builder_linux_android*
id_rsa
id_rsa.pub
id_ed25519
id_ed25519.pub
*.pem
*.ppk

# Secrets
.env
.env.*
*.local
*.local.*
secrets/
secrets.*
SECRETS*
*.keystore
*.jks
*.p12
*.pfx
key.properties
android/key.properties
google-services.json
GoogleService-Info.plist

# Claude Code / AI assistant — never commit
.claude/
CLAUDE.md
CLAUDE.local.md

# gitleaks scan output (run locally, don't commit results)
gitleaks-report.*
.gitleaks-baseline*

# Flutter auto-generated (leaks local dev paths)
.flutter-plugins
.flutter-plugins-dependencies
.metadata

# Lock-Datei des lokalen Plugin-Pakets — die App-Lock-Datei ist maßgeblich.
packages/*/pubspec.lock
# ⚠️ Spiegel des Server-PHP. Der Code lebt NUR auf dem Server; diese
# Verzeichnisse sind Arbeitskopien und tragen Zugangsdaten und Schluessel.
# Das Repo ist OEFFENTLICH — fehlt hier ein Eintrag, legt der naechste
# `git add -A` ihn in die Auslage.
#
# ⚠️ MUSTER STATT LISTE — UND ZWAR, WEIL DIE LISTE NACHWEISLICH VERSAGT HAT.
# Bis 16.08.2026 stand hier eine von Hand gepflegte Aufzählung. Sie war
# lückenhaft: `_server_web360s/` (511 Dateien, 17 MB), `_server_kontakt/`,
# `_server_signatur/`, `_server_webstat/`, `_server_finanz/` und
# `_server_sms_inbox/` fehlten — sechs von dreizehn. Nachgetragen wurden sie
# in #271, wieder von Hand; dieses Muster ersetzt die Pflege durch eine Regel,
# die auch für den nächsten Spiegel gilt, den jemand anlegt.
#
# Die Anweisung „wer einen neuen Spiegel anlegt, trägt ihn im selben Commit
# hier ein" war genau die, die sechsmal nicht befolgt wurde. Eine Regel, die
# nur wirkt, solange alle daran denken, wirkt irgendwann nicht mehr — und man
# merkt es erst, wenn der Ordner öffentlich ist.
#
# ⚠️ Kehrseite, bewusst in Kauf genommen: ein Verzeichnis `_server_…`, das
# WIRKLICH ins Repo soll, wäre damit still ausgeschlossen. Es gibt keins —
# geprüft, kein einziger `_server_*`-Pfad ist getrackt — und der Präfix meint
# in diesem Projekt ausschließlich Serverspiegel.
_server_*/
